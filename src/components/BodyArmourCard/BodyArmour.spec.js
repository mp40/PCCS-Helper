import React from 'react';
import { shallow } from 'enzyme';
import BodyArmourCard from './index';

describe('the BodyArmourCard', () => {
  // eslint-disable-next-line react/jsx-filename-extension
  const wrapper = shallow(<BodyArmourCard />);
  it('should...', () => {
    // todo
  });
});
